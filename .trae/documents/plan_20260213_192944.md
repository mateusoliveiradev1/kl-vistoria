Vou ajustar as dimensões da logo para que ela fique com o tamanho ideal e bem visível.

### **Ajustes Planejados**

1.  **Aumentar Logo no Cabeçalho (Header)**
    *   Aumentar a altura de `h-12` (48px) para `h-20` (80px) ou `h-24` (96px).
    *   Ajustar o preenchimento (padding) do cabeçalho para acomodar o logo maior sem ficar apertado.

2.  **Aumentar Logo no Rodapé (Footer)**
    *   Aumentar significativamente de `h-16` (64px) para `h-32` (128px) ou mais, para dar o destaque que a marca merece.

3.  **Verificação de Proporção**
    *   Garantir que a propriedade `object-contain` continue sendo usada para que o logo nunca distorça (estique ou esmague), mantendo suas proporções originais perfeitas.

### **Arquivos Afetados**
*   `src/components/Header.tsx`
*   `src/components/Footer.tsx`
